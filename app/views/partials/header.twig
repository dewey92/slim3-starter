<nav class="navbar navbar-primary navbar-fixed-top white{% if home %} transparent{% endif %}" role="navigation">
	<div class="container-fluid">
		<!-- Brand and toggle get grouped for better mobile display -->
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#top-nav">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="${ urlFor('home') }">
				<img src="/public/images/logo-circle.png" alt="Katanium">
			</a>
		</div>

		<!-- Collect the nav links, forms, and other content for toggling -->
		<div class="collapse navbar-collapse" id="top-nav">

			<ul class="nav navbar-nav navbar-left">
				<li>
					<a href="editor">Editor Picks</a>
				</li>
				<li>
					<a href="top_stories">Top Stories</a>
				</li>
				<li>
					<a href ui-sref="trending">Trending</a>
				</li>
			</ul>

			{% if auth %}
				<ul class="nav navbar-nav navbar-right">
					<li>
						<a href="#" class="navbar-btn btn-link dropdown-toggle" id="user-dropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
							<img src="/user-uploads/${ auth.user_id }/${ auth.avatar }" id="logged-user-pp" class="img-circle" alt="${ auth.displayName }" height="34" />
						</a>
						<ul class="dropdown-menu wider" data-uid="${ auth.user_id }" data-displayname="${ auth.displayName }">
							<li class="dropdown-item">
								<ul class="list-unstyled text-center">
									<li>
										<a href="${ urlFor('user', { 'displayName' : auth.displayName }) }" title="${ auth.displayName }'s photo">
											<img src="/user-uploads/${ auth.user_id }/${ auth.avatar }" alt="User photo" class="img-circle" width="90"/>
										</a>
									</li>
									<li>
										<a href="${ urlFor('user', { 'displayName' : auth.displayName }) }" class="normal">${ '@' ~ auth.displayName }</a>
									</li>
									<li>
										<a href="${ urlFor('user', { 'displayName' : auth.displayName }) }" class="normal">${ auth.fullName }</a>
									</li>
									<li>
										<ul class="list-inline">
											<li>
												<a href="${ urlFor('user', { 'displayName' : auth.displayName }) }" class="btn btn-sm btn-primary rounded" role="button">Profile</a>
												<a href="${ urlFor('dashboard') }" class="btn btn-sm btn-primary rounded" role="button">Dashboard</a>
											</li>
										</ul>
									</li>
								</ul>
								<hr/>
							</li>
							<li class="dropdown-item notif">
								<div class="text-center">
									<h5>NOTIFICATION</h5>
									<ul class="list-unstyled">
										<li>Oops! Not implemented yet</li>
									</ul>
								</div>
							</li>
							<li role="separator" class="divider"></li>
							<li><a href="${ urlFor('logout') }">Logout</a></li>
						</ul>
					</li>
				</ul>
				{% if write %}
					<div class="btn-group">
						<a href="#" class="btn btn-success rounded navbar-btn dropdown-toggle" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Post!</a>
						<ul class="dropdown-menu">
							<li><a href="#" id="publish">Publish</a></li>
							<li><a href="#">Save</a></li>
							<li role="separator" class="divider"></li>
							<li><a href="#">Discard</a></li>
						</ul>
					</div>
				{% else %}
					{% if not dashboard %}
						<ul class="nav navbar-nav navbar-right">
							<li>
								<a href="${ urlFor('write') }" class="btn navbar-btn rounded{% if home %} btn-white{% else %} btn-primary{% endif %}" role="button" style="padding: 6px 10px">
									<i class="fa fa-pencil"></i>
								</a>
							</li>
						</ul>
					{% endif %}
				{% endif %}
			{% else %}
				<a href="#login-modal" class="btn navbar-btn rounded{% if home %} btn-white{% else %} btn-primary{% endif %}" data-toggle="modal" role="button">Sign in / Sign up</a>
			{% endif %}

			<form class="navbar-form navbar-right" action="" role="search">
				<div class="form-group has-feedback">
					<input type="text" name="search" v-model="searchText" class="form-control rounded" placeholder="Search" aria-describedby="aria-search-input">
					<span class="fa fa-search form-control-feedback" aria-hidden="true"></span>
					<span id="aria-search-input" class="sr-only">Search everything</span>
				</div>
			</form>

		</div><!-- /.navbar-collapse --></div>
</nav>
